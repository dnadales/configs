[user]
	email = damian.nadales@iohk.io
	name = Damian Nadales
	signingkey = 5D4FE8505464AB1E

[core]
	editor = emacsclient
        excludesfile = ~/.gitignore

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[diff]
        tool = meld
[commit]
        template = ~/.gitmessage
	gpgsign = true
[merge]
	conflictstyle = diff3
