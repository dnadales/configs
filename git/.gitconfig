[user]
	email = damian.nadales@iohk.io
	name = Damian Nadales
	signingkey = F2F6EDCDB0DBEE24
[core]
	editor = emacsclient
    excludesfile = ~/.gitignore
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[diff]
    tool = meld
[commit]
    template = ~/.gitmessage
	gpgsign = true
